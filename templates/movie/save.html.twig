{% extends 'base.html.twig' %}

{% set label = movie.id ? 'Edit Movie' : 'New Movie' %}

{% block title %}{{ label }}{% endblock %}

{% block body %}
<div class="example-wrapper">
    <h1>{{ label }}</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Send</button>
    {{ form_end(form) }}
</div>
{% endblock %}
